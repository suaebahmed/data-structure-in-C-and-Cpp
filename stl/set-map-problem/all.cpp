#include<bits/stdc++.h>

using namespace std;
#define FORI(a,n) for(int i=a; i<n; i++)
#define FOR(n) for(int i=0; i<n; i++)

#define first fi // does't work
#define second se
#define map<int,int> mp //does't work

int main()
{
    int n;
    cin>>n;
    map<string,int> msi;

    return 0;
}
